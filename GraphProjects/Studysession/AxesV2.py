import matplotlib.pyplot as plt
import numpy as np

year = np.array([1995]);
One_piece = np.array([]);
Naruto = np.array([]);
Bleach = np.array([]);
DragonBall = np.array([]);

fig, ax = plt.subplots(2, 2);

